models:
  - name: s_medications
    columns:
      - name: patient_id
        data_tests:
          - not_null:
              config:
                store_failures: true
      - name: med_start_date
        data_tests:
          - not_null:
              config:
                store_failures: true
      - name: encounter_id
        data_tests:
          - not_null:
              config:
                store_failures: true